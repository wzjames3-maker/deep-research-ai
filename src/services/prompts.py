PLAN_GENERATION_PROMPT = """你是一个研究计划生成器。根据用户的研究主题和模板类型，将研究拆分为 3-5 个子课题（sub-agent）。

## 模板拆分策略
- **tech_research**: 技术原理 → 生态与社区 → 竞品对比 → 应用场景 → 发展趋势
- **competitive_analysis**: 市场定位 → 产品矩阵 → 定价策略 → 用户口碑 → 优劣势对比
- **literature_review**: 经典理论 → 近年进展 → 争议与讨论 → 研究空白 → 方法论评估
- **custom**: 自由拆分 3-5 个合理角度

## 输出要求
请严格按以下 JSON 格式输出，不要包含其他文字：

```json
[
  {{
    "name": "子课题名称",
    "goal": "子课题研究目标（一句话）",
    "searchDirection": "首轮搜索关键词"
  }}
]
```

## 输入
- 研究主题: {topic}
- 模板类型: {template}"""


PLAN_REVISION_PROMPT = """你是一个研究计划修改器。根据用户的反馈意见，修改当前的研究计划。

## 当前计划
{current_plan}

## 用户反馈
{feedback}

## 输出要求
请严格按以下 JSON 格式输出修改后的计划，不要包含其他文字：

```json
[
  {{
    "name": "子课题名称",
    "goal": "子课题研究目标（一句话）",
    "searchDirection": "首轮搜索关键词"
  }}
]
```

## 输入
- 研究主题: {topic}"""


RELEVANCE_FILTER_PROMPT = """你是一个搜索结果相关性评估助手。给定研究主题和搜索方向，对以下搜索结果进行相关性评分。

## 研究主题
{topic}

## 搜索方向
{direction}

## 搜索结果
{results}

## 输出要求
对每条结果评分 0-10，只返回 JSON 数组：

```json
[
  {{"index": 0, "score": 8, "reason": "简要说明"}},
  {{"index": 1, "score": 3, "reason": "广告页面，无实质内容"}},
  ...
]
```

## 评分标准
- 9-10: 直接回答研究问题，来自权威来源
- 7-8: 高度相关，包含有价值的具体数据或分析
- 5-6: 部分相关，有一定参考价值
- 3-4: 边缘相关，信息较泛或来源不可靠
- 0-2: 不相关、广告、内容农场、纯导航页"""


QUERY_EXPANSION_PROMPT = """你是一个搜索策略助手。根据研究主题和搜索方向，生成 2 个不同角度但互补的搜索查询变体。

## 研究主题
{topic}

## 搜索方向
{direction}

## 输出要求
严格返回 JSON 格式：

```json
{{"queries": ["变体查询1", "变体查询2"]}}
```

## 查询变体策略
- 变体 1: 加入限定词（如年份、机构、具体指标）
- 变体 2: 换角度（如从技术角度转到应用/对比/趋势角度）
- 每个变体不超过 15 个单词/30 个中文字"""


SUB_AGENT_SEARCH_PROMPT = """你是一个研究分析助手。根据搜索方向分析新的搜索结果，判断信息是否充足，并输出更新后的研究发现。

## 研究主题
{topic}

## 当前发现
{findings}

## 新搜索结果
{search_results}

## 搜索方向
{direction}

## 输出要求
请严格按以下 JSON 格式输出：

```json
{{
  "findings": "更新后的研究发现（Markdown 格式，包含关键信息和引用来源）",
  "sufficient": true/false,
  "new_search_query": "下一轮搜索关键词（如 sufficient=false）"
}}
```

## findings 字段写作规范（重要！）
1. **信息密度优先**：每个信息点用 2-3 句话描述核心内容，包含具体数据、年份、机构名称等细节
2. **结构化组织**：使用 Markdown 二级/三级标题分组，不同维度的信息分开讨论
3. **来源标注**：每个信息点后用 `[来源](URL)` 标注出处
4. **最低 800 字**：单轮 findings 不得少于 800 中文字符，包含至少 5 个独立信息点
5. **累加而非替换**：将本轮新发现与已有 findings 合并，保留旧内容，补充新内容
6. **仅当信息确实充分时才设 sufficient=true**：如果搜索结果未覆盖核心问题，应继续搜索

## 噪音识别与质量控制
1. **忽略低质量结果**：广告、SEO 填充页、内容农场、纯导航页面应标记为无效来源，不纳入 findings
2. **来源可信度评估**：优先引用知名机构官网、学术期刊、官方文档；对个人博客、论坛帖子保持审慎
3. **交叉验证**：对于重要数据点（市场规模、技术性能等），需要至少 2 个不同来源交叉确认
4. **标注信息可信度**：在 findings 中对每条关键信息标注「已确认」「待验证」「存疑」

## new_search_query 生成策略
当 sufficient=false 时，new_search_query 应：
1. 基于当前 findings 的**信息缺口**生成更精准的查询，而非简单重复原搜索方向
2. 尝试不同角度的查询词（如加入限定词：年份、机构、对比、案例等）
3. 如果当前结果太泛，尝试加入具体术语或限定范围"""


AGGREGATE_PROMPT = """你是一个研究报告撰写助手。根据研究计划和各子课题的研究发现，生成一份完整的 Markdown 研究报告。

## 报告结构要求
1. **研究计划摘要**（research plan recap）：简要回顾研究目标和子课题划分
2. **各子课题详细发现**：每个子课题单独一节，包含具体发现和原始来源链接
3. **综合结论**（synthesis & key findings）：提炼关键发现和建议
4. **参考文献**（references）：在报告末尾列出所有引用的来源

## 引用规则（重要！）
以下是可用的引用编号和对应的来源 URL：

{citation_map}

**引用格式要求**：
- 在报告中引用某个来源时，必须使用 `[N]` 格式（如 `[1]`、`[3]`）
- 引用编号放在相关事实或数据点的句子末尾，标点符号之前
- 每个关键信息点都应标注引用来源
- 同一信息可以有多个引用（如 `[1][3]`）
- 仅使用上述引用映射中列出的编号，不要编造编号

## 研究计划
{plan}

## 各子课题研究发现
以下是从各子课题（sub-agent）收集的研究发现：

{sub_agent_findings}

## 输出要求
- 使用 Markdown 格式
- 报告最大长度：50,000 中文字符
- 每个信息点必须用 [N] 格式标注引用编号
- 报告末尾添加 `## 参考文献` 部分，列出所有被引用的来源
- 语言专业、客观、结构清晰

## 参考文献部分格式
```markdown
## 参考文献

[1] 标题 — URL
[2] 标题 — URL
...
```

## 输入
- 研究主题: {topic}"""
